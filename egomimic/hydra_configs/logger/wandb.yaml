# https://wandb.ai

wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  # name: "" # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  id: "${name}_${description}_${now:%Y-%m-%d_%H-%M-%S}" # pass correct id to resume experiment!
  anonymous: null # enable anonymous logging
  project: ${oc.env:WANDB_PROJECT, "everse_denoiser_tests"}
  log_model: False # upload lightning ckpts
  prefix: "" # a string to put at the beginning of metric keys
  entity: ${oc.env:WANDB_ENTITY, null} # set to name of your wandb team
  group: ${oc.env:WANDB_GROUP, ""}
  tags: []
  job_type: ${oc.env:WANDB_JOB_TYPE, ""}
